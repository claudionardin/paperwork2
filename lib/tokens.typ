// Design tokens. Every size, colour and font used by the templates lives here.
// TODO replace with the brand values once config/company.yaml carries a colors block.

#let font-body = ("Noto Sans",)          // vendored in typst/assets/fonts and embedded in the binary
#let font-mono = ("Noto Sans",)

#let size-body = 9.5pt
#let size-small = 8pt
#let size-footer = 6.5pt
#let size-title = 18pt
#let size-h1 = 13pt
#let size-h2 = 11pt

#let ink = rgb("#1a1a1a")
#let muted = rgb("#6b6b6b")
#let rule = rgb("#c8c8c8")
#let accent = rgb("#1a1a1a")
#let band = rgb("#f2f2f2")

#let page-margin = (top: 22mm, bottom: 24mm, left: 18mm, right: 18mm)
